[pytest]
# Suppress upstream importlib DeprecationWarnings from third-party C-extension
# loaders — not actionable in our code.
filterwarnings =
    ignore::DeprecationWarning:importlib.*

markers =
    integration: marks tests as integration tests requiring live API keys or R2 credentials (deselect with '-m "not integration"')
