--extra-index-url https://download.pytorch.org/whl/cpu

# Core app dependencies
requests>=2.31.0
feedparser>=6.0.10
openai>=1.55.0
sentence-transformers>=3.0.0
pandas>=2.0.0
google-genai>=0.3.0
groq>=0.9.0
tenacity>=8.2.3
boto3>=1.26.0
scikit-learn>=1.3.0
adapters>=0.2.0
yake>=0.4.8
torch==2.7.0+cpu

# VectorDB — LanceDB on R2 (replaces faiss-cpu + bm25s)
lancedb>=0.6.0
pyarrow>=12.0.0
pylance>=0.30.0

# FastAPI backend (F-03) — serves backend/pipeline_core.py's pipeline over SSE
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
